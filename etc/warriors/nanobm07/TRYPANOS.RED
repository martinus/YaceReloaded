;redcode-nano verbose
;name Trypanosoma brucei
;author Fluffy
;strategy Need a short sleep?
;assert CORESIZE == 80

        ORG start

start   spl.b  # 34, > 66
        mov.i  # 10, > 61
        mov.i  > 55, } 78
        mov.i  # 19, > 13
        mov.i  #  1, $  1

        END
